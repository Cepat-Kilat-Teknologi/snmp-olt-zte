apiVersion: v1
kind: Service
metadata:
  name: snmp-olt-zte
  namespace: snmp-olt-zte
  labels:
    app.kubernetes.io/name: snmp-olt-zte
    app: snmp-olt-zte
spec:
  selector:
    app.kubernetes.io/name: snmp-olt-zte
  ports:
    - name: http
      port: 8081
      targetPort: http
      protocol: TCP
  type: ClusterIP
